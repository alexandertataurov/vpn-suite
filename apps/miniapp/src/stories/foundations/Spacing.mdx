import { Meta, Canvas, Story } from "@storybook/addon-docs/blocks";
import { FoundationHeader, FoundationLinks, DoDont } from "./components";
import * as SpacingStories from "./Spacing.stories";

<Meta of={SpacingStories} />

<FoundationHeader
  title="Spacing"
  description="8px grid, insets, gaps, and layout rhythm."
  status="Stable"
/>
<FoundationLinks tokens="src/design-system/core/tokens/spacing.ts" />

## Hero rationale

Consistent spacing creates visual rhythm and alignment. The 4px base scale (4, 8, 12, 16, 20, 24, 32, 40, 48) keeps layouts predictable.

## Live showcase

<Canvas>
  <Story id="foundations-spacing--showcase" />
</Canvas>

## Rules

- Use `--spacing-*` or `--space-*` tokens
- Do not hardcode px values in components
- Touch targets: minimum 48px for interactive elements

## Do / Don't

<DoDont
  do={<span>Use <code>--spacing-*</code> tokens for padding and gaps</span>}
  dont={<span>Hardcode px values like <code>17px</code> in layout components</span>}
/>

## Token inventory

<Canvas>
  <Story id="foundations-spacing--scale" />
</Canvas>

## Interactive Playground

<Canvas>
  <Story id="foundations-spacing--playground" />
</Canvas>

## Applied UI Examples

<Canvas>
  <Story id="foundations-spacing--showcase" />
</Canvas>

## Implementation notes

- Tokens: `--spacing-1` (4px) through `--spacing-12` (48px)
- Aliases: `--space-*` maps to `--spacing-*`
- Component padding: `--spacing-component-padding`, `--spacing-section-gap`

## Accessibility

- Touch targets: minimum 48×48px for interactive elements
- Spacing between targets: at least 8px

## Related

- Foundations / Layout
- Foundations / Accessibility
