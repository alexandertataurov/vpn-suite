import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Introduction/Overview" />

<DocPage>
  <DocHero
    label="Overview"
    title="Presentation-grade Storybook, not a component graveyard"
    description="This Storybook is built to explain the system, not just display it. Tokens, rules, and usage guidance should stay obvious so teams can ship UI without guesswork."
    version="v2.0"
  />

  <DocCallout icon="◈" variant="rule">
    Treat Storybook as the operating manual for the UI layer. It should answer what exists, when to use it,
    and what the contract is before anyone starts improvising in production.
  </DocCallout>

  <DocDivider label="01 — What this Storybook is for" />

  <DocSection>
    <DocGrid columnCount={3}>
      <DocRule title="Foundations first" tag="tokens">
        Start with design tokens, semantics, and governance rules before looking at assembled UI.
      </DocRule>
      <DocRule title="Patterns over screenshots" tag="process">
        Patterns and showcase stories explain how the system behaves in real product situations.
      </DocRule>
      <DocRule title="Walkthrough-friendly" tag="walkthrough">
        Docs should hold up in stakeholder demos, design reviews, and implementation handoff.
      </DocRule>
    </DocGrid>
  </DocSection>

  <DocDivider label="02 — How to navigate it" />

  <DocSection>
    <DocChecklist
      items={[
        "Start with Foundations to understand tokens, theming, and the core governance rules.",
        "Move to Patterns and Components to see how those rules become real UI behavior.",
        "Use showcase stories for walkthroughs because they carry applied examples and more stable interactions.",
      ]}
    />

    <DocCallout icon="→" variant="note">
      The fastest path is simple: <strong>learn the rules once</strong>, then reuse the same patterns everywhere.
      That is cheaper than rediscovering spacing, states, and composition on every screen.
    </DocCallout>
  </DocSection>

  <DocDivider label="03 — Sources of truth" />

  <DocSection>
    <DocGrid columnCount={2}>
      <DocRule title="Tokens" tag="source">
        <code>src/design-system/core/tokens</code> and <code>src/design-system/styles/</code> define the visual system.
      </DocRule>
      <DocRule title="Story governance" tag="stable">
        <code>storybook:taxonomy</code> and <code>storybook:contract</code> enforce naming and contract discipline in the miniapp package.
      </DocRule>
      <DocRule title="UI framing" tag="ui">
        <code>src/storybook/StorybookMiniappShell.tsx</code> controls the presentation shell around the docs environment.
      </DocRule>
      <DocRule title="Applied documentation" tag="mdx">
        Curated MDX pages should add rationale, do/don't guidance, and real usage context — not duplicate Autodocs noise.
      </DocRule>
    </DocGrid>
  </DocSection>
</DocPage>
