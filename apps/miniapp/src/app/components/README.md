# App-layer components

Product-specific UI that should not live in the generic design-system barrel (see [`index.ts`](index.ts)). For reusable primitives and compositions, use [`design-system/`](../../design-system/README.md).
