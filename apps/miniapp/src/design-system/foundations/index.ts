/**
 * Design-system foundations: tokens + theme runtime.
 * Use this entrypoint when UI code needs design decisions, not components.
 */
export * from "../core/tokens";
export * from "../core/theme";
