/**
 * Loading strategy: route-level code-split (lazy) + Suspense.
 * One skeleton component per layout segment; no ad-hoc spinners in features.
 * Skeleton component is provided by design-system (Phase 2).
 */

export const ROUTE_LOADING_FALLBACK_KEY = "route-loading";
