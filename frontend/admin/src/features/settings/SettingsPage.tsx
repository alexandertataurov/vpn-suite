import { SectionTitle, MetaText } from "@/design-system";

export function SettingsPage() {
  return (
    <div className="page settings-page">
      <SectionTitle>Settings</SectionTitle>
      <MetaText>Settings — Phase 1 placeholder.</MetaText>
    </div>
  );
}
