# Migration Strategy

1. Adopt new primitives from `ui/primitives` in all new work.
2. Migrate admin and miniapp components incrementally.
3. Replace legacy tokens with new semantic tokens.
4. Remove deprecated components after adoption completes.
