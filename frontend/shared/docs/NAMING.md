# Naming Conventions

- Primitives are exported as `PrimitiveX` from `@vpn-suite/shared/ui`.
- Composite components use domain-neutral names (`Table`, `HealthBadge`).
- Token names use `--color-*`, `--spacing-*`, `--text-*`, `--radius-*`, `--shadow-*`.
