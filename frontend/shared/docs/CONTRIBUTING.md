# Design System Contribution Rules

- Use semantic tokens only (`--color-*`, `--spacing-*`, `--text-*`, `--radius-*`).
- No inline styles in primitives or composites.
- Use existing primitives before creating new components.
- Variants and sizes must be union types.
- Document all changes in Storybook.
