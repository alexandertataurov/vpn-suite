import { LabeledRow } from "../../patterns";

export type { LabeledRowProps as LabeledControlRowProps } from "../../patterns";

/** Labeled control row recipe. Re-exports LabeledRow pattern. */
export const LabeledControlRow = LabeledRow;
