import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as RadiusStories from "./Radius.stories";
import { FoundationHeader, FoundationLinks } from "./components";

<Meta of={RadiusStories} />

<FoundationHeader
  title="Radius"
  description="Border radius tokens for consistent corner rounding."
  status="Stable"
/>
<FoundationLinks tokens="src/design-system/core/tokens/radius.ts" />

## Why it exists

Consistent border radius creates visual harmony and defines surface elevation. Use tokens instead of arbitrary values.

## Rules

- Use `--radius-*` tokens for all rounded corners
- Do not hardcode border-radius values
- Match radius to component size (small components = smaller radius)

## Token inventory

<Canvas of={RadiusStories.Scale} />

## Implementation notes

- Tokens: `--radius-sm`, `--radius-md`, `--radius-lg`, `--radius-xl`, `--radius-full`
- Use `--radius-full` for pills and avatars

## Related

- Foundations / Shadows
- Foundations / Color
