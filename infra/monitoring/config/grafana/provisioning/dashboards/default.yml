apiVersion: 1
providers:
  - name: VPN Suite
    orgId: 1
    folder: VPN Suite
    folderUid: vpn-suite
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
